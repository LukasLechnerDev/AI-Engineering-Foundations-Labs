# 20 - Model choice and routing

## Goal
- Teach students that model selection is a system design decision, not just an API default.

## What to cover
- matching model capability to task complexity
- using smaller models for narrow subtasks
- reserving stronger models for harder reasoning tasks
- routing different requests to different models
- when one-model-for-everything is a bad product design
