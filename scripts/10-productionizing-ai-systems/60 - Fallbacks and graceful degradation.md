# 60 - Fallbacks and graceful degradation

## Goal
- Show students how an AI feature can remain useful even when the ideal path fails.

## What to cover
- fallback models
- fallback prompts
- deterministic fallback logic
- when to skip a feature instead of blocking the whole workflow
- user experience when confidence is low or a stage fails
